
let mod
    console = builtIn.js.console
    node = builtIn.js.node
    js = builtIn.js
    interval = builtIn.js.interval
    timeout = builtIn.js.timeout
in
    val nodeMain = node.fromSuper $
        (interval.set 500 $ console.log "Hello everyone!")
        <js.bind> (\id =
            console.log id
            <js.bind> (\n = timeout.set 6000 $
                console.log "That's all!"
                <js.bind> (\n = interval.clear id)
            )
        )
