#
    This module demonstrates usage of modules
#

let mod
    unit where
        pro new
in let val
    unit = unit.new
    identity v = v
in let mod
    bool file bool
    maybe file maybe
in let mod
    either file either
    list file list
in val jsMain = builtIn.js.console.log "Hello, world!"